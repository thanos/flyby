# flyby-benches

Criterion benchmark harnesses for FlyBy. Every optimization must have a
benchmark (design principle #4). Harnesses live under `benches/` and are
run with:

```sh
cargo bench -p flyby-benches
```

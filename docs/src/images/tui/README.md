# TUI screenshots

SVG/text captures of `flyby-sim tui`, generated by:

```bash
cargo run -p flyby-simulator --example render_tui_docs
```

| File | Scenario | Notes |
|---|---|---|
| `01-paused-constant-rate` | `constant_rate` | Fresh paused frame |
| `02-packet-loss-stepped` | `packet_loss` | After ~25 steps; drops visible |
| `03-queue-overflow` | `queue_overflow` | Small ring / pressure demo |

Do not edit the SVG/TXT by hand — regenerate from the example.
